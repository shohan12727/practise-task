
const withAllVowels = "Just imagine a quiet voice echoing above a tree.";
const withoutVowels = "Hymn spry crypts fly by my shy gypsy lynx myths.";


console.log(withAllVowels.includes('a' || 'e' || 'i' || 'o' || 'u'));
console.log(withoutVowels.includes('a' || 'e' || 'i' || 'o' || 'u'));

