const fruits = ["Apple", "Banana", "Mango", "Orange", "Grapes"];

const [ele1, ele2, ele3] = fruits;


console.log(ele3);


const person = {
  name: "Shohan",
  age: 20,
  profession: "Student",
  hobbies: ["coding", "reading", "gaming"],
  isAvailable: true
};

const keys = Object.keys(person);
const values = Object.values(person) 
console.log(keys, values);











// const {name, age, profession, hobbies, isAvailable} = person;

// console.log(name, age, profession, hobbies[0], isAvailable);






// console.log(JSON.stringify(person));

